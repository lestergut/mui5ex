const Donor = () => {
   return (
      <div>
         <h1>Donor</h1>
         {/* {Data.map((info) => {
            //const { title, subtitle, image, alt, resumee, content } = info;
            return <KwnoledgeUnit information={info} key={info.id} />;
         })} */}
      </div>
   );
};

export default Donor;
